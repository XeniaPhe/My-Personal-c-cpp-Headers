#include <stdlib.h>
#include <stdio.h>

//Change the DataType struct and the two constants to use with a different data type
struct DataType
{
    int data;
} typedef Data;

const Data defaultData = {0};
const char dataChar = 'd';